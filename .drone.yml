kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64

steps:
  - name: kaniko
    image: plugins/kaniko
    settings:
      username: kevinbacon
      password: pa55word
      repo: "registry.cluster/p2p/drone-test"
      tags: latest
    extra_hosts:
      - "registry.cluster:192.168.1.3"

  - name: push
    image: plugins/docker
    settings:
      repo: "p2p/drone-test"
      registry: registry.cluster
      insecure: true
      #username:
      #  from_secret: docker_username
      #password:
      #  from_secret: docker_password
    when:
      event:
        include:
          - push

trigger:
  branch:
    - main
